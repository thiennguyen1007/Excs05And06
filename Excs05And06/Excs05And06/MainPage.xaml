<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Excs05And06.MainPage"
             xmlns:IMGcrl="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             Title="Thông báo">
    <StackLayout>
        <ScrollView>
            <ListView ItemsSource="{Binding ListNhanVien}" ItemSelected="ListView_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Grid ColumnDefinitions="*,4*">
                                    <IMGcrl:CircleImage Source="{Binding IMG}" Grid.Column="0"/>
                                    <Label Grid.Column="1" Text="{Binding Desc}"/>
                                </Grid>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>
    </StackLayout>
    

</ContentPage>
